apiVersion: v1
kind: Service
metadata:
  name: recommendations-svc
  namespace: ledgerline
spec:
  selector:
    app: recommendations
  ports:
    - port: 8002
      targetPort: 8002
  type: ClusterIP   # internal only — called by ledger, not exposed externally
